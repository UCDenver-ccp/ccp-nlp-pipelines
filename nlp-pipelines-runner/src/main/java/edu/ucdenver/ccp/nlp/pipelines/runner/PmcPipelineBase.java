package edu.ucdenver.ccp.nlp.pipelines.runner;

/**
 * Interacts with a run catalog. Instances use a run key. The pipeline searches
 * the catalog for documents that are missing the run key and subsequently
 * processes them.
 */
public abstract class PmcPipelineBase extends PipelineBase {

}
